# service example do it on boot to reboot and mv works
# use start and exec just to make shore it will RUN
on boot
    start isu
    exec /sbin/launch_daemonsu.sh 

service isu  /system/bin/sh /sbin/isu.sh
    class main
    user root
    seclabel u:r:init:s0
    disabled
    oneshot
